# HU2 - Tests for HU1

Branch: `feature/HU2-tests`

Contenido:
- Tests para `EventController` y `VenueController` usando MockMvc.
- Validación: prueba que enviar un nombre vacío devuelve 400.
- README corto con instrucciones.

Cómo ejecutar tests:

Usa Maven desde la raíz del proyecto:

```bash
mvn test
```

Descripción breve: se agregaron tests básicos (happy paths y validación) para dejar la HU1 cubierta con pruebas automáticas iniciales.
